datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "../../../node_modules/@prisma/roadsign-client"
}

enum RoadSignType {
  existing
  planned
  pending
}

enum RoadSignStatus {
  todo
  doing
  done
}

/// 路牌朝向：ew/we/ns/sn 为双向，*_one 为单向
enum RoadSignDirection {
  ew
  we
  ns
  sn
  ew_one
  we_one
  ns_one
  sn_one
}

enum RoadSignLevel {
  l1
  l2
}

/// 昌都市区道路名录（下拉字典）
model Road {
  id        Int      @id @default(autoincrement())
  name      String   @unique @db.VarChar(100)
  sort      Int      @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([sort])
  @@index([name])
}

/// 昌都路牌点位
model RoadSign {
  id          Int               @id @default(autoincrement())
  name        String            @db.VarChar(200)
  /// 所属道路（对应 Road.name）
  roadName    String            @default("") @db.VarChar(100)
  description String            @default("") @db.VarChar(1000)
  extra       String            @default("") @db.Text
  lng         Float
  lat         Float
  type        RoadSignType
  status      RoadSignStatus    @default(todo)
  direction   RoadSignDirection
  /// 单向指向距离（米）；双向为空
  distanceM   Int?
  level       RoadSignLevel
  /// 路牌主图：{ id, kind, url, originalUrl, name? } | null
  signPhoto   Json?
  /// 附带照片数组
  extraPhotos Json              @default("[]")
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  @@index([status])
  @@index([type])
  @@index([name])
  @@index([roadName])
}

/// 管理员当前登录会话：新登录会覆盖 sessionId，旧 JWT 立即失效
model AdminSession {
  username  String   @id @db.VarChar(64)
  sessionId String   @db.VarChar(64)
  updatedAt DateTime @updatedAt
}
