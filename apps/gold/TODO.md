# Gold 服务后续 TODO（未做项）

> 防止遗忘。原则：**先确认 Tick 原料可信，再沉淀分析结果。**  
> 已做：Tick 采集（墙上时钟）、周末休市停采（UTC 五 22:00→ 日 22:00）、**日线按 UTC 22:00 交易日界聚合**（与 london-gold 对齐；历史 currency-api 不迁移）、日线冻结、`/api/features/swing`（现算不落库）、`DailyQuality`（开市时长期望）、`PollLog` 采价失败/skip。

最后更新：2026-08-23

---

## 暂缓 · 完整结构特征包（原「第 1 点」扩全）

**为何先不做 / 先不存**

- 要先靠 `DailyQuality` 确认 tick 覆盖率、断档、冻结态可信。
- 原料不稳时把特征落库，会把「假平静 / 假波段」当成历史真相。

**以后要做（按需现算 → 质量稳定后再考虑按日摘要落库）**

- [ ] 振幅分布（元/克或 %）
- [ ] 回撤深度统计
- [ ] 趋势段长度
- [ ] 支撑 / 阻力簇
- [ ] 时段活跃度（亚洲 / 伦敦 / 纽约等）
- [ ] 触发：累计 N tick 或满 K 根 5m·H1 后可出报告（仍建议只读 JSON）
- [ ] **持久化策略（仅当要复盘「某日特征面板」时）**：按日（或日+参数）存**精简摘要**，不要每次 `/features/swing` 全量写库
- [ ] 产出给：策略页 / 波段 Tab / 进化日志对照（**不**直接改仓位规则、不自动进化调参）

**已有代替**

- `GET /api/features/swing`：正向低 → 高腿，按需计算，不落库。

---

## 已完成 · 日终质量（原「第 2 点」）

- [x] 表 `DailyQuality`（每天一行）
- [x] 覆盖率 / 断档 / `tick:final` / currency-api 收盘偏差
- [x] 期望条数按开市毫秒计（周六半日 / 周日休市 / 周一晚开）
- [x] `UTC 22:01` 冻结后写入 + 启动补写上一交易日
- [x] `GET /api/features/quality` · `POST /api/features/quality/run`

**可选增强（仍属质量，非结构特征）**

- [x] 桌面端展示日终质量（london-gold「服务日志」Tab）
- [x] 采价失败 / skip 落库 `PollLog` + `GET /api/poll-logs`（短窗合并，约留 7 天）
- [ ] 连续多日覆盖率告警（通知或日志）
- [ ] 周汇总：平均覆盖、断档趋势
- [ ] 桌面「更多日志」：结构摘要 / poll 错误（可接 `/api/poll-logs`）/ collect-log

---

## 明确不做（除非另开需求）

- [ ] 服务端自动改策略参数并上线
- [ ] 服务端可执行买卖信号推送
- [ ] 把完整策略引擎迁上云
- [ ] 高频把每次波段计算结果全量落库

---

## 迭代闭环提醒

```text
Tick + DailyQuality（可信原料）
  → 决策/成交/进化日志（本机）
  → AI 主导：目标吃波段最大化收益
  → 数据够才改规则并写进化日志
```

游戏北星（桌面）：`london-gold/docs/game-north-star.md`
